# 聊天链路回归手册（CHAT-REGRESSION）

> 用途：任何**动到聊天链路**的改动（引擎 / 状态机 / 聊天页 UI / Markdown / 分支 / 输入岛）在提交前
> 按本手册走一遍。**自动化部分一条命令**，人工部分逐项走查并留证据。
> 对应设计真源：`docs/DESIGN-compose.md` §19（验收标准）与 §14/§18（形态规格）。

---

## 0 · 设备纪律（先看这条）

| 规则 | 说明 |
|---|---|
| **仪器化测试只跑模拟器** | 必须显式指定：`ANDROID_SERIAL=emulator-5554`。`connectedDebugAndroidTest` 默认会在**所有已连接设备**上安装并执行——首次跑就把测试件指向了真机（AGP 报告里的设备名 `A9210`）。 |
| **真机只装 release 包** | 真机（小米，adb 序号 `df97f3c4`；另有 `PA921BMGL3190210G`）不装 debug 包、不装测试件。 |
| 门禁兜底 | `checkChat` / `connectedDebugAndroidTest` 前置任务 `verifyEmulatorDevice` 会检查 `ANDROID_SERIAL` 必须是 `emulator-*`，否则**直接失败**。确需多设备（不含真机）用 `-PallowAllDevices=true` 显式放行。 |

---

## 1 · 自动化（一条命令）

```bash
ANDROID_SERIAL=emulator-5554 ./gradlew checkChat
```

它做三件事：
1. `verifyEmulatorDevice` —— 设备门禁（只允许模拟器）；
2. `testDebugUnitTest` —— **304 例**纯逻辑单测（引擎 / 检索 / 工具 / 标记过滤 / 用量 / 分支模型 /
   消息与状态机 / Markdown 解析）；
3. `connectedDebugAndroidTest` —— **7 例**仪器化 UI 测试（见下）。

| UI 用例 | 覆盖的产品契约 |
|---|---|
| 未配置供应商时点发送会弹配置对话框 | 配置缺失**不静默失败** |
| 输入为空时发送键不可用输入后可用 | 发送键状态可视 |
| 流式生成后正文上屏且出现用量脚注 | 假传输驱动下：正文上屏 + **用量脚注真的渲染**（usage 从引擎一路到 UI） |
| 生成失败时出现错误卡且不插入消息 | 错误**不进消息列表**（不污染对话与楼层统计） |
| 删除消息先弹确认框 | 破坏性操作先确认（**含用户消息**） |
| 世界书面板展示真实条目 | 只读面板接的是真实数据源 |
| 工具面板开关切换后说明文案变化 | 工具开关状态与说明联动 |

> 假传输（`FakeTransport`）让流式/失败用例**不依赖网络**——真实供应商只在人工走查里跑。

---

## 2 · 人工走查（十步，自动化未覆盖的必须走）

| # | 步骤 | 期望 | 自动化？ |
|---|---|---|---|
| 1 | 冷启动 | 落在最新一轮（开页贴底） | 否（依赖 Activity 冷启动） |
| 2 | 唤出键盘 / 收起 | 输入岛完整可见、不被键盘压住 | 否 |
| 3 | 发送（真实供应商） | 逐字流式 → 思考节点自动展开/流式/收起 → 完成后常驻摘要 | 部分（假传输覆盖渲染；**真实 SSE 节奏**需人工） |
| 4 | 工具开关 A/B | 开启：出现「调用工具」节点；关闭：不出现（请求体不带 tools） | 否（需真实端点） |
| 5 | 停止 / 重新生成 | 停止保留已到达正文；重新生成出现 `‹2/2›` 且可来回切 | 否 |
| 6 | 上滑回看 | 自动跟随停止 → 回底按钮出现（有新内容带圆点）→ 点击回底 | 部分（按钮渲染已覆盖；**手势与跟随**需人工） |
| 7 | 切剧情分支 | 对话内容整体更换、统计随之变化 | 否 |
| 8 | 编辑 / 删除 | 编辑用户消息 → 确认式重跑；删除（单条 / 及其后）走确认 | 部分（删除确认已自动化） |
| 9 | 断网发送 | 出现错误卡；消息列表与分支统计**不被污染** | 否（需真实断网） |
| 10 | 亮/暗 + 150% 系统字号 | 上述 1–9 不破版 | 否 |

**留证据**：截图存 `docs/design/verify-*.png`；流式节奏用 `adb logcat -s LuzzyStream`（每个增量一行）。

---

## 3 · 写 UI 测试时的三个坑（已踩过，别再犯）

1. **弹窗/菜单是独立窗口**：`assertIsDisplayed` 在弹窗刚出现时可能假红 → 先 `waitUntil(...)` 等节点出现。
2. **LazyColumn 里滚出视口的条目不在语义树里**：断言前先 `onNodeWithTag("chat_list").performScrollToNode(...)`；
   直接用 `waitUntil { 节点存在 }` 会**永远等不到**。
3. **testTag 不能掺中文/开关状态**：用 `slot_tools` 而不是 `slot_工具（已开启）`（后者会随文案与状态变化，
   测试跟着碎）。另外 `hasScrollAction()` 在聊天页会匹配到**两个**滚动容器（消息列表 + 输入岛的横向功能行），
   定位列表必须用 tag。

---

## 4 · 接真实存储之后新增的四个坑（P4-B-3.4 实测，都很贵）

> 前三条都不是「语法错」，而是**测试写法**问题：它们让测试**假红**，且报错信息指向完全无关的地方。
> 记录在此，避免下次又花半小时从「Room 是不是坏了」开始查。

1. **`Thread.sleep` 轮询会饿死帧**：聊天页的落盘协程跑在 `rememberCoroutineScope()`
   （AndroidUiDispatcher，**由帧驱动**）上。测试线程一旦 `Thread.sleep` 阻塞轮询，就没人推进帧 →
   协程永不执行 → 现象是「界面上消息出来了，但库里 8 秒都没写进去」，拆卸时还留下一条
   `JobCancellationException`（看起来像数据库坏了）。
   **正确写法**：用 `compose.waitUntil { runBlocking { 查库 } }` 等（帧照常推进）；
   条件里**只查库、不要调 Compose API**（调了会撞 `performMeasureAndLayout called during measure layout`）。
2. **`performTextInput` 是追加不是替换**：编辑弹窗里要改已有文字，必须先
   `performTextClearance()`，否则得到「原标题新标题」拼起来的字符串。
3. **别用 `substring = true` 做「某条消息可见」的断言**：样例里「分支首句」会让
   `hasText("首句", substring = true)` 命中主线的断言 → 假绿。要精确匹配。
4. **一个测试回合可能追加两条消息**：发送会依次落「用户消息」+「助手回复」。
   断言「条数 == 之前 + 1」只在两次追加之间的一瞬成立 → 会假红。
   **改成按内容找**（`messages.any { it.content == "…" }`）而不是数数。

另加两条纪律：

- **UI 测试必须注入存储与假传输**：不注入就会去读设备上真实的 `luzzy.db`（初始界面随机器状态变）并
  发真实网络请求（拖慢用例、把 activity 卡在 PAUSED）。两个接缝：`ChatPage(sessionRepository = …)`、
  `ChatPage(engineFactory = …)`。
- **每个用例一个独立临时库**：否则上一个用例写进去的消息会改变下一个用例的初始界面，
  失败长得像「UI 坏了」，实际是测试互相污染。
