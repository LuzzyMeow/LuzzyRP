# HANDOFF · P5 下一轮提示词（会话 73 → 74）

> ⛔ **本文件已过期（会话 74 收尾时标记）——不要照它做。**
> 它写的三件事都已完成或作废：① 真机序号写成 `PA921BMGL3190210G`（实为备用机，
> 真实日常机是 `df97f3c4`）；② 「恢复模拟器数据集」的步骤本身是错的（预推的夹具会被
> 迁移页覆盖，见 WORKLOG 节点节）且用户已指示不用模拟器；③ §11 的 A6/A7 重做方案
> 已执行完毕。**当前交接看 `docs/HANDOFF-p5-batch-b.md`。**
> 下面内容仅作历史追溯保留。

> **用法**：整段复制「下一轮提示词」发给下一个 Agent 即可。
> 本文件是**交接文档**：它只写「下一个 Agent 不知道但必须知道的事」，
> 已沉淀进仓库的内容只给路径不重复。

---

## 给下一轮 Agent 的提示词（整段复制）

```
继续 LuzzyRP 的 P5 工作。接手前按顺序读三份文档：

1. `docs/WORKLOG.md` 末尾的「📍 当前工作节点（会话 73 暂停）」——当前状态与我造成的
   环境损坏及恢复步骤；
2. `docs/PLAN-v3.0-p5-agent-loop.md` ——本轮主计划（§1 DSH 架构证据、§3 批 A 详案、
   §11 执行暂停点与 A6/A7 重做方案三步）；
3. `docs/PLAN-v3.0-p5-agent-loop.md` 附录（上游消费链，带 file:line）。

背景一句话：P5 的「生效」（预设/世界书接进请求组装）走到批 A 暂停点——
纯函数层与 A4/A5 已完成并推送（HEAD 532b6ecc），
A6/A7（快照落盘成历史消息）因两处真缺陷已回退，重做方案三步写在 PLAN §11。

已拍板的决策（不要再问）：
- 注入策略 = DSH 式尾部快照（4 个 position 保真进 system、3 个漂移型改尾部快照）；
- 工作区入口撤掉；五个假数据页全部接真库；设计门继续豁免三方向。

你的任务（按序）：
1. 恢复模拟器数据集（我上一轮误卸载了应用，数据清空；恢复步骤在 WORKLOG 节点节的
   「下一步 1」——用夹具 JSON 推到迁移入口目录，root 可用）；
2. 干净重启模拟器后跑 `ANDROID_SERIAL=emulator-5554 ./gradlew checkChat`，
   确认回到全绿基线（若偶发红，重启模拟器再跑一次——长跑模拟器的环境劣化，
   WORKLOG 有记录）；
3. 按 PLAN §11 三步重做 A6/A7（快照落盘位置修对：先落快照、再落用户消息；
   payload 标记持久化 + 界面不渲染但进请求）；
   验收加字节级断言：turn1 请求 ⊂ turn2 请求；
4. A7 观测层 `CacheObserver.kt`（公共前缀比 + cached_tokens，只统计不干预）
   + A8 用量页命中率列；
5. 批 A 验收（PLAN §7）：模拟器连续多轮实测公共前缀 ≥0.95 + 负控
   （改一次设置应掉 <0.5 并记 headerChanged）。

纪律提醒（本仓库的硬性规定，接手必读 `AGENTS.md` + `HARD_REQUIREMENTS.md`）：
- 每个工作项先写测试再写实现；单跑绿的用例必须连跑 3 次确认稳定才算过
  （概率性门禁比没有更糟——本会话两次踩这条）；
- 我造过两次需要你注意的现场：①模拟器数据被清（恢复步骤见上）；
  ②`docs/PLAN-v3.0-p5-agent-loop.md` §11 记录了 A6/A7 回退的全部原因与方案；
- `built-in-content.js` 的 nsfw_rules 永不可碰；上游文件只走登记 patch；
- 真机（用户将连接，小米 A9210 / adb PA921BMGL3190210G）**只装 release 包**，
  覆盖安装步骤见 `DESIGN-compose §20.4`；真机数据是用户真实数据，写之前必须先问。

完成后：每批独立提交 + 跑门禁 + WORKLOG 记录 + push。
```

---

## 给用户的真机测试指引（你操作时参考）

1. **连真机**：USB 连接小米 A9210，确认 `adb devices` 里出现 `PA921BMGL3190210G`。
2. **装包**：下一轮 Agent 完成 A6/A7 重做并跑绿门禁后，让它构建
   `./gradlew :app:assembleRelease` 并
   `adb -s PA921BMGL3190210G install -r app/build/outputs/apk/release/app-release.apk`
   （同签名覆盖安装，数据保留）。
3. **重点试**（批 A 的验收点，模型对位置敏感，必须人眼确认）：
   - 角色是否按**你的角色卡人设**说话（此前一直用演示人设 Vanio——这是本次修掉的最大的错）；
   - 预设开关后回复风格是否变化；
   - 世界书条目命中后回复是否用到对应设定；
   - 连续多轮对话是否流畅（缓存命中应让后续轮次更快）。
4. **回滚**：若 v3.0 包不可用，直接卸载 v3.0 重装 v1.4.0 APK（真机数据是 v3.0 迁移过来的，
   回滚会丢 v3.0 之后的新对话——装前可先在应用内导出角色卡/世界书备份）。

---

## 交接清单（下一轮 Agent 开工前核对）

| 项 | 状态 |
|---|---|
| 纯函数层（PromptAssembler/WorldBookActivator/PromptSections/RuntimeSnapshots） | ✅ 已提交，51 JVM 测试绿 |
| WorldBookTool 换真库 + schema 字节稳定门禁 | ✅ 已提交 |
| 温度/最大输出持久化修复 | ✅ 已提交（d5e008ff） |
| A6/A7 快照落盘 | ⚠️ 已回退，重做方案 PLAN §11 |
| CacheObserver 观测层 + 用量页命中率 | 未开始（A7/A8） |
| 五个假数据页接真库 | 未开始（C1） |
| 真实角色图 / 多候选持久化 / 附件 / 表格列宽 / 撤工作区 / reduced-motion | 未开始（C2-C7） |
| 字号 / 导入导出 / 迁移报告页 / 文档反向修正 | 未开始（D1-D4） |
| 模拟器数据集 | ⚠️ 已清空，恢复步骤在 WORKLOG 节点节 |
| 完整 Agent Loop（批 B） | 未开始，要素清单在 PLAN §1.4 |
