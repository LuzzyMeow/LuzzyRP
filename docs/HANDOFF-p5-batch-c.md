# HANDOFF · 批 C 提示词（会话 75 → 76）

> ⚠️ **本文件已被 `docs/HANDOFF-p5-static.md` 取代（2026-09-14）**：设备被用户收回，
> 下一轮**没有真机**，只做静态工作。本文件保留作历史追溯（其中的真机步骤在设备回来时仍有效）。

> **用法**：整段复制下面「下一轮提示词」发给下一个 Agent 即可。
> 本文件只写「下一个 Agent 不知道但必须知道的事」；已沉淀进仓库的内容只给路径不重复。
> **上一份交接 `docs/HANDOFF-p5-batch-b.md` 已执行完毕**（批 B 代码收口）——不要再照它做。

---

## 给下一轮 Agent 的提示词（整段复制）

```
继续 LuzzyRP 的 P5 工作（批 B 已收口，进入批 C）。接手前按顺序读四份东西：

1. `docs/WORKLOG.md` 末尾的「📍 当前工作节点（会话 75 暂停 · 批 B 代码完成，真机未验收）」
   —— 当前状态、批 B 逐项状态、未修缺陷、真机环境坑；
2. `docs/PLAN-v3.0-p5-agent-loop.md` §5（批 C 详案）、§7（验收标准）、§12（进度勾选）；
3. `docs/DESIGN-compose.md` §24（请求组装与缓存）与 §25（Agent Loop）
   —— 本轮刚回填的设计真源，改这两块之前必读；
4. `app/src/main/java/com/luzzymeow/luzzyrp/chat/AgentLoop.kt` 的类注释
   —— B1/B2/B5/B6 的设计依据（DSH 证据行号）与「为什么这样而不是那样」。

背景一句话：批 A（请求组装 + 前缀缓存）已真机验收达标（公共前缀 1.0000、命中 89%）；
批 B（完整 Agent Loop）代码全部落地（B1-B7），JVM 单测 588 条 / 0 失败连跑 3 次稳定，
**并已在真机抽验**（`df97f3c4`，机上就是 HEAD `062796ad` 那份 release 包）：
B5 压缩真机跑通（`压缩落库 idx=4 裁=4 条 · 估算 13943→12153`、该轮提示词 10,847→9,288、
缓存命中 80.8%），同时抓到并修掉四处真缺陷（三处「流式不跟随」+ 一处配置入口缺失）。
`062796ad` 又补上了**正文 HTML 直通**（模型输出的 `<div>/<table>/<style>` 卡片现在是真卡片渲染，
安全边界：JS/网络/文件全关 + 白名单清洗 + CSP），设计门走了豁免②并过独立评审 GO（需调整，六项必修已全做）。
细节见 `docs/WORKLOG.md` 会话 75 §10/§11（含设备操作经验：弹窗按钮会被软键盘盖住、
`input keyevent` 逐位数字是改文本框的可靠方式）。

已拍板的决策（不要再问）：
- 注入策略 = DSH 式尾部快照（4 个 position 保真进 system、3 个漂移型改尾部快照）；
- 工作区入口撤掉；五个假数据页全部接真库；设计门继续豁免三方向；
- 真机 = `df97f3c4`（小米 25098PN5AC / Android 16，用户日常机，**只装 release 包**）；
- `contextWindow`（B5 的门槛）默认 32768、填 0 = 关闭自动压缩；
- 「流式不跟随」按**用户手势**判跟随（`followTail`），不再逐帧算几何判据。

你的任务（按序）：
1. 先跑一次 `./gradlew :app:testDebugUnitTest` 确认 588 条全绿（接手基线）；
2. **补完上游正文渲染链的另两条机制**（`DESIGN-compose §26.6`）——HTML 直通已于 `062796ad` 完成，
   同族的这两条还没有消费方，用户已就「显示层没还原」提过两次，**优先于批 C**：
   - **① 显示期正则**：上游 `runtime-services.js` 的 `applyDisplayRegex` → `processRegex(text, {isDisplay:true, role})`，
     用户的正则脚本应在**显示前**改写正文（用户报的「引号内文字高亮没了」即此项）；
     `rp_hub_regex` 记录已迁移进库但**没有任何消费方**，等于记录了不生效。
   - **② UI 模板变量块**：模型输出 JSON 变量块 → 渲染模板面板 + 从正文里剥掉该块
     （`findUiTemplateUpdateBlock` / `parseUiTemplateUpdates` / `renderUiTemplateHtml` / `globalUiTemplates`，
     现状是 `!set_color` 这类控制行当普通文字显示出来）。
   *为什么先做*：与刚做完的 HTML 直通同属一条链、同一处代码（`ui/markdown/` + 消息渲染），
   改动面小、用户能直接看见差别；等批 C 做完再回来要重新加载上下文。
   做法参照 §26 的既有结论：纯函数解析（先写测试）+ 渲染侧只消费解析结果 + 安全边界不放松。
3. **批 C 第 1 项：五个假数据页接真库**（PLAN §5-C1）——关于（版本走 BuildConfig + CHANGELOG 真源）／
   用量（records(usage) + 折线 + **缓存命中率列**，数据源已在 `CacheObserver`）／
   记忆（memories 真统计 + 清空真执行）／角色卡（真列表 + 真实头像 + 切换 + 删除）／
   设置（用户档案真绑定 → 喂 A2 用户信息块；供应商配置接 `TransportStore`）；
   *为什么先做它*：它是「界面可信度」的总开关，也是把批 A/B 挣来的观测数据（命中率、用量）
   真正摆到用户面前的一步；
4. 其余批 C 项按 PLAN §5-C2…C7 顺序做（每项一条断言，**可见性判据优先于数据判据**）；
5. 跑门禁（JVM 3 连跑）+ 独立提交 + WORKLOG 记录 + push。

**批 C 之后是 P6「切换与发版」**（`docs/PLAN-v3.0-compose.md`）：现状是 **launcher 仍指向 WebView 的
`MainActivity`**，Compose UI 只能 `adb am start -n com.luzzymeow.luzzyrp/.ui.ComposeActivity` 进；
P6 要切 launcher、移除 WebView 路径、按 AGENTS §3.4 出**单个 APK** + 同签名 + 真机回归。
**别在批 C 里顺手切 launcher**——那是发版动作，要用户在场。

纪律提醒（本仓库硬性规定，接手必读 `AGENTS.md` + `HARD_REQUIREMENTS.md`）：
- **每个工作项先写测试再写实现**；单跑绿的用例必须**连跑 3 次**确认稳定才算过；
- **不要启动模拟器**（用户明确要求）；`checkChat` 因此跑不了，门禁用 `:app:testDebugUnitTest`；
- 真机写操作前先问用户；**不要再动真机输入法设置**；
- **真机 UI 点击的三条经验**（会话 75 §10-④ 有完整经过）：① 坐标必须取自**当次** dump
  （有无软键盘两种布局相差约 300px）；② 弹窗按钮会被键盘盖住 → 先发 `input keyevent 66` 收键盘再点；
  ③ 改文本框用**逐位数字 keyevent**（0=7、1=8 … 9=16），`input text` 会经 IME 插入垃圾字符；
- `built-in-content.js` 的 nsfw_rules 永不可碰；上游文件只走登记 patch；
- 一条概率性的门禁比没有更糟——判据必须确定性。

**四件遗留（都不阻塞上面两项）**：
① B6 重试的**真机**触发（要真断网/真溢出，无法按需造）——JVM 侧 12 例已覆盖；
② 「末项远高于视口」的长回复跟随只由机制与手势判据保证（本轮生成长度不足以把气泡顶出视口）；
③ 用户会话主分支里有一条压缩水位线（会话 75 §10-⑤ 的副作用）——**清它会连带删掉其后消息，
   必须用户明确同意**；用户没提就别动；
④ HTML 卡片的**已接受风险**（`§26.7`，设计评审记录在案、本轮不修）：每卡一个 WebView 的滚动帧率
   （需「同会话 10 张卡」A/B 才能定性）、模型给卡片写的颜色不随主题、WebView 抢占长按选择
   （复制得到的是 HTML 源码）。
```

---

## 交接清单（下一轮开工前核对）

| 项 | 状态 |
|---|---|
| 批 A（请求组装 / 前缀缓存 / 观测层 / 命中率） | ✅ 完成并真机验收（公共前缀 1.0000、命中 89%） |
| 批 B B1-B4 / B7 | ✅（B4 的 `interrupted` 只有数据没有界面） |
| 批 B B5 压缩 | ✅ 代码 + 落库水位线 + 设置项；**真机已跑通**（裁 4 条 / 提示词 10.8K→9.3K / 命中 80.8%） |
| 批 B B6 重试 | ✅ 代码；真机**无法按需触发**（JVM 12 例覆盖） |
| 「流式不跟随」三处缺陷 | ✅ 真机定位 + 修复 + 复验（`2c747503`） |
| 供应商配置入口（已配置后不可达） | ✅ 已补并可进（真机验证） |
| `contextWindow` 设置项 | ✅ 默认 32768 / 填 0 关闭 / 非法输入拦保存（真机验证错误态与保存） |
| 两个存量真缺陷（配置保存丢字段 / 追加覆盖老消息） | ✅ 已修（前者真机验证其余字段不被重置） |
| `DESIGN-compose §24/§25` 回填 | ✅ 已完成（§15.3 同步更新） |
| 正文 HTML 直通（上游卡片渲染） | ✅ `062796ad`；设计门豁免②+独立评审 GO（需调整，六项必修全做）；真机双主题目测通过 |
| `DESIGN-compose §26` 回填 | ✅ 26.1 上游三条通道 / 26.2 实现 / 26.3 安全 / 26.4 容器视觉与设计门记录 / 26.5 真机证据 / 26.6 未实现的两条 / 26.7 必修项与残余风险 |
| 显示期正则（`applyDisplayRegex`） | ❌ 未实现（`rp_hub_regex` 已入库无消费方；用户报的「引号内文字高亮没了」）——**下一轮第一件事** |
| UI 模板变量块（`uiTemplateUpdates`） | ❌ 未实现（`!set_color` 类控制行当前当普通文字显示）——**下一轮第一件事** |
| 批 C（五个假数据页 / 真实角色图 / 多候选持久化 / 附件 / 表格列宽 / 撤工作区 / reduced-motion） | ❌ 未开始 |
| 批 D（字号 / 导入导出 / 迁移报告页 / 文档反向修正） | ❌ 未开始 |
| 仪器化测试（`checkChat`） | ⚠️ 按用户指示未跑（无模拟器）；源码编译通过 |
| 真机上的包 | ✅ `062796ad`（批 A + 批 B + 跟随修复 + HTML 直通），同签名覆盖、数据保留 |
| 用户会话里的压缩水位线 | ⚠️ 主分支 idx=4 有一条（B5 验证的副作用，见 WORKLOG §10-⑤；清理须用户同意） |
| launcher / 发版（P6） | ❌ 仍指向 WebView `MainActivity`；Compose UI 目前只能 `adb am start` 进 |
