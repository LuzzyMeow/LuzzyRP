<?xml version="1.0" encoding="utf-8"?>
<!-- LuzzyRP 主清单（v3.0 P6 起：唯一界面为 Compose；WebView 仅剩迁移通道的隐藏用法） -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 网络访问：用户配置的 LLM API 端点请求；迁移页（files/ext/）资产加载 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- [v3.0 P6 移除] WRITE_EXTERNAL_STORAGE（DownloadHandler 专用，已随 WebView 路径退役删除，
         导入导出全部走 SAF 无需权限）；
         READ/WRITE_CALENDAR（已移除的「助手」日历工具专用，功能不存在即无权限必要） -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.LuzzyRP"
        tools:targetApi="37">

        <!-- [v3.0 P6] launcher 切换：Compose 界面成为唯一入口。
             P1-P5 期间 launcher 挂在 WebView MainActivity 下（「点图标进老界面」的根因），
             P6 随 WebView 路径退役移交本 Activity 并删除 MainActivity 声明。 -->
        <activity
            android:name=".ui.ComposeActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|density|fontScale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
