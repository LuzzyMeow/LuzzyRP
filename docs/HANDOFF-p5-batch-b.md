# HANDOFF · 批 B 续做提示词（会话 74 → 75）

> **用法**：整段复制下面「下一轮提示词」发给下一个 Agent 即可。
> 本文件只写「下一个 Agent 不知道但必须知道的事」；已沉淀进仓库的内容只给路径不重复。
> **上一份交接 `docs/HANDOFF-p5-next.md` 已过期**（它写的真机序号、模拟器恢复步骤、
> §11 重做方案都已完成或作废）——不要再照它做。

---

## 给下一轮 Agent 的提示词（整段复制）

```
继续 LuzzyRP 的 P5 批 B 工作。接手前按顺序读四份东西：

1. `docs/WORKLOG.md` 末尾的「📍 当前工作节点（会话 74 暂停 · 批 B 在途 · 供下次接手直接续）」
   —— 当前状态、批 B 做到哪、未修缺陷、真机环境注意事项；
2. `docs/PLAN-v3.0-p5-agent-loop.md` §4（批 B 详案）、§7（验收标准）、§11（批 A 暂停点与真机记录）、
   §12（进度勾选，批 B 的勾选状态是本轮刚更新的真实状态）；
3. `app/src/main/java/com/luzzymeow/luzzyrp/chat/AgentLoop.kt` 的类注释
   —— 它写清了 B1/B2/B3 的设计依据（DSH 证据行号）与「为什么这样而不是那样」；
4. `docs/PLAN-v3.0-presets-worldbook.md` 附录 A（上游消费链，带 file:line）。

背景一句话：批 A（请求组装 + 前缀缓存）已完成并**真机验收达标**（公共前缀 1.0000、
该轮缓存命中 89%）；批 B「完整 Agent Loop」走了一多半 —— B1/B2/B3/B4/B7 已落地并有单测，
**B5（压缩）与 B6（重试）未开始**。HEAD `ffd37a2c`（已 push），工作区干净，
JVM 单测 **509 条 / 0 失败**。

已拍板的决策（不要再问）：
- 注入策略 = DSH 式尾部快照（4 个 position 保真进 system、3 个漂移型改尾部快照）；
- 工作区入口撤掉；五个假数据页全部接真库；设计门继续豁免三方向；
- 真机 = `df97f3c4`（小米 25098PN5AC / Android 16，用户日常机，**只装 release 包**）。

你的任务（按序）：
1. 先跑一次 `./gradlew :app:testDebugUnitTest` 确认 509 条全绿（这是接手基线）；
2. 做 **B5 压缩**：`contextWindow` 进 `TransportConfig`（默认保守值 + 设置页/提示文案）、
   触发阈值 `tokens >= contextWindow × 0.8`、保留 system 头 + 最近 `retainRatio`(0.16) 尾部、
   **不切开 tool_call/tool_result 配对**、摘要请求**按原前缀原样重放**（吃一次缓存）、
   压缩记事件。先写测试再实现。
3. 做 **B6 重试**：网络错误与 context-overflow 各重试一次（后者先压缩）。
4. 跑门禁（JVM 3 连跑）+ 独立提交 + WORKLOG 记录 + push；
5. 回填 `DESIGN-compose §24（请求组装与缓存）` 与 `§25（Agent Loop）`——这是 PLAN §6-D4 的欠账。

纪律提醒（本仓库硬性规定，接手必读 `AGENTS.md` + `HARD_REQUIREMENTS.md`）：
- **每个工作项先写测试再写实现**；单跑绿的用例必须**连跑 3 次**确认稳定才算过；
- **不要启动模拟器**（用户明确要求）；`checkChat` 因此跑不了，门禁用 `:app:testDebugUnitTest`；
- **不要用 `adb shell input text` / `input keyevent` 驱真机**（打不进 Compose 输入框，
  会话 74 在这上面耗了很久）；能用的是 `input tap` + `uiautomator dump` 取坐标；
  需要真机发消息就请用户发；
- **不要再动真机输入法设置**（会话 74 诊断时动过，已恢复；见 WORKLOG 节点节）；
- 真机数据是用户真实数据：**写之前先问**；
- `built-in-content.js` 的 nsfw_rules 永不可碰；上游文件只走登记 patch；
- 一条概率性的门禁比没有更糟（会话 74 实撞：`CotParserTest` 用挂钟卡 1ms，
  本机同时编译 + 装机时假红）——判据必须确定性。

**在动批 B 之前，先处理这一条**：用户实测反馈「真流式输出失效 / 划到底部不自动吸附跟随」
**未定位**，而会话 74 恰好改过 `send`/`runTurn`/列表项，**是首要嫌疑**。
最省事的定性办法是与上一版 v3.0 包做 A/B（`git stash` 或 `git worktree` 出
`d400e766` 那份构建，装到真机让用户发 1-2 条消息对比）。拿到结论前**不要**把它们写进文档
当「既有缺陷」或「已修」。
```

---

## 交接清单（下一轮开工前核对）

| 项 | 状态 |
|---|---|
| 批 A（请求组装 / 前缀缓存 / 观测层 / 命中率） | ✅ 完成并真机验收（公共前缀 1.0000、命中 89%） |
| 两个启动期真缺陷（旧设置搬运标记 / 首帧竞速） | ✅ 已修 + 真机复验 |
| 输入法抬起输入岛 | ✅ 已修 + 真机复验（y 2465→1481→2465） |
| `ChatEngine` → `AgentLoop` 改名 | ✅ 已做（**勿再引入 `ChatEngine`**） |
| B1/B2 循环与终止 | ✅ 18 条单测 |
| B3 配对 + 合成结果 + 工具抛异常兜底 | ✅ |
| B3b 工具轨迹落库（`luzzyToolTrail`） | ✅ |
| B4 中断标记落库/认回 | ✅（UI 显示未做） |
| B5 压缩 | ❌ 未开始（`TransportConfig` 无 `contextWindow`） |
| B6 重试 | ❌ 未开始 |
| `DESIGN-compose §24/§25` 回填 | ❌ 欠账 |
| 用户 4 条实测反馈（流式/吸附/正则高亮/首轮 CoT） | ⚠️ 已分诊，**未修**；前两条需 A/B 定性 |
| 仪器化测试（`checkChat`） | ⚠️ 按用户指示未跑（无模拟器）；`ChatPersistenceTest.setContent` 有一处**已知的注入缺口**（未注入 promptInputSource 等，属隐藏耦合，会话 74 发现但**未改**，因为改完跑不了验证） |
| 真机上的包 | ⚠️ 是 `d400e766`（批 A + 输入法修复），**不是** `ffd37a2c`（批 B）。批 B 是引擎重写，**装真机前先问用户** |
