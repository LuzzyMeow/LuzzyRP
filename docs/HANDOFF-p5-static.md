# HANDOFF · 无真机轮次（会话 75 → 76）

> **用法**：整段复制下面「下一轮提示词」发给下一个 Agent 即可。
> 本文件只写「下一个 Agent 不知道但必须知道的事」；已沉淀进仓库的内容只给路径不重复。
> **上一份 `docs/HANDOFF-p5-batch-c.md` 已被本文件取代**（那份假设有真机；本轮起**没有真机**）。

---

## 给下一轮 Agent 的提示词（整段复制）

```
继续 LuzzyRP 的 P5 工作。**本轮没有真机**（用户把 `df97f3c4` 收回了），所以只做静态工作：
判据必须全部落在 `./gradlew :app:testDebugUnitTest` 上。不要尝试 adb / 装包 / 截图 / 仪器化运行；
`adb devices` 里若出现别的设备也不是目标机（此前出现过第二台 `PA921BMGL3190210G`），不要拿它验收。

接手前按顺序读四份东西：

1. `docs/WORKLOG.md` 末尾「📍 当前工作节点（会话 75 收尾 · 设备已被用户收回）」—— 状态一览、
   **正文渲染链完成度总表**（判断「显示不对」先看这张表）、缺陷清单、以及按「有没有真机」分好的下一步；
2. `docs/DESIGN-compose.md` §26（块级 HTML 直通）与 §27（显示期正则 + 行内 HTML）
   —— 这两节是正文渲染链的设计真源，改渲染之前必读（§27.6 列了本节**没做**的部分）；
3. `app/src/main/java/com/luzzymeow/luzzyrp/chat/RegexScripts.kt` 的类注释
   —— 显示期正则的**逐条上游出处表**（哪条语义对应 `app.js` 哪一行）与两处有意偏离；
4. `docs/PLAN-v3.0-p5-agent-loop.md` §5（批 C 详案）/ §7（验收标准）/ §12（进度勾选）。

当前基线：JVM 单测 **647 条 / 0 失败**（连跑 3 次稳定）；机器上装过的最新 release 包含
批 A + 批 B + §26 卡片 + §27 正则/行内，但 §27 的**目视验收没做完**（设备在验收中途被收回）。

你的任务（按序，全部是静态可判定的）：

1. 先跑 `./gradlew :app:testDebugUnitTest` 确认 647 条全绿（接手基线）。
2. **A1 提示词侧正则接线**：把已实现的 `RegexScripts.apply(..., Mode.Prompt, depth)` 接进
   `RequestBuilder` 的历史消息映射（上游 `app.js:6511-6518`：`depth = 条数 - 1 - 下标`）。
   判据：`RequestBuilderTest` 新增「仅提示词脚本进请求 / 仅用户可见脚本不进 / depth 区间外跳过」，
   并**重跑批 A 的纯追加不变量**（脚本是确定性的 → 前缀仍必须稳定）。
   注意：这条会改请求内容，**真机复核缓存命中率**要写进真机清单（本文件 B 栏），别声称已验证。
3. **A2 `<ui_template_updates>` 变量块剥除**（上游 `data-services.js:1650-1691`）：
   只认「受保护区之外的最后一块」、未闭合算到文末、块内 JSON 解析容忍 ``` 围栏与单/多模板两种形态。
   判据：纯函数单测（剥除后正文不含块、块内 JSON 不参与正则/行内 HTML、非法 JSON 不炸界面）。
   面板渲染（`renderUiTemplateHtml`）**不要做**——那要真机。
4. **A3 思考块（CoT）拆分**：`parseCot`（`core-utils.js:19-68`）的完整移植 →
   `ranges/cot/main/closingTags/isFinished`（含流式未闭合的收尾规则）。
   判据：纯函数单测（嵌套 thinking、未闭合、代码围栏里的标签不算）。渲染成节点的视觉留真机。
   这条同时是缺陷 #4（「首轮正文出现 CoT」）的纯逻辑一半。
5. **A5 批 C 的取数层与纯逻辑**（**不碰视觉验收**）：用量聚合（`records(usage)` + 缓存命中率）、
   记忆真统计、角色列表/头像路径、设置绑定（用户档案 → A2 用户信息块；供应商 → `TransportStore`）。
   判据：单测 +「同输入同输出」。页面视觉与「用户看得见」判据留真机（仓库被咬过一次：
   「DOM 里有 ≠ 用户看得见」）。
6. **A6/A7**：把新增能力写成仪器化用例并保证 `:app:compileDebugAndroidTestKotlin` 通过（运行留真机）；
   顺手把本文件 B 栏扩成**可勾选的真机回归清单**（步骤 / 预期 / 判据）。
7. 跑门禁（JVM 3 连跑）+ 独立提交 + WORKLOG 记录 + push。

**要问用户的三件事（不要自己拍板）**：
① 文风过滤 `filterBlockedStyleText` 要不要做（它**删改 AI 措辞**，上游默认开；属产品行为）；
② 主分支里那条压缩水位线是否清掉（清它会连带删掉其后消息）；
③ `NAI画图正则` 若被启用，会渲染出一张**没有图**的生图卡片（图靠上游 JS 管线填、卡片网络被禁）
   —— 要不要先显式跳过它。

**别做**（无真机时做了就是堆积未验收改动）：P6 切 launcher / 发版；批 C 的页面视觉验收声明；
任何「已目视验收」的说法；也不要为了「正文里出现 `!set_color`」发明过滤器——上游同样把它当普通文字
（那是模型自造的控制行，见 `DESIGN-compose §26.6②`）。
```

---

## A 栏 · 无真机可做（判据都在 JVM 门禁上）

> **会话 76 已全部做完**（✅ 见下表状态列）。逐项落点与判据：
>
> | # | 任务 | 状态（会话 76） |
> |---|---|---|
> | A1 | 提示词侧正则接进 `RequestBuilder` | ✅ `RegexScripts.applyToPromptMessages` + `RequestBuilder.plan(regexScripts, promptUserName)`；`RequestBuilderTest` +7 例；**纯追加不变量已重跑**，并把「深度区间会漂移」写成负面代价用例 |
> | A2 | `<ui_template_updates>` 块剥除 + JSON 解析 | ✅ `chat/UiTemplateUpdates.kt`（纯函数，28 例）+ 接进 `MessageBody`/`UserBubble`（**剥块在正则之前**）；面板渲染按指示**未做** |
> | A3 | `parseCot` 完整移植 | ✅ `CotParser` 补 `ranges`/`rawCot`/`closingTags`；**顺带收编** `RegexScripts.cotRanges` 的重复实现（受保护区自此单一真源）；+13 例 |
> | A4 | 文风过滤 | ✅ **用户拍板「做，默认开，照上游」** → `chat/StyleFilter.kt`（22 例，期望值全部由上游正则真跑取得）；接在显示链末尾、只对 AI 消息 |
> | A5 | 批 C 取数层/聚合纯逻辑 | ✅ `chat/PageAggregates.kt`（纯）+ `chat/PageDataSource.kt`（取数，22 例，含真实夹具）；三个页面已接真库（角色/记忆/用量） |
> | A6 | 仪器化用例补齐 + 编译通过 | ✅ `PageDataUiTest`（7 例）+ `MessageBodyUiTest`（9 例）；`:app:compileDebugAndroidTestKotlin` 通过；**运行留 B4** |
> | A7 | 真机回归清单成形 | ✅ 见下方「B 栏 · 可勾选真机回归清单」 |
>
> **另**：用户拍板 `NAI画图正则` **显式跳过**（`RegexScripts.IMAGE_GEN_UNSUPPORTED`），
> 避免渲染一张永远不出图的空卡片；开关是一行常量，生图管线（C4）做好后翻它即可。

## B 栏 · 可勾选的真机回归清单（设备回来时按序执行）

> **执行纪律**：① 每项都要**用户在场或同意**（写数据项尤其）；② 用真机（`df97f3c4`）+
> **release 签名包**（`./gradlew :app:assembleRelease` → `adb -s df97f3c4 install -r …`，
> 同签名覆盖、数据保留）；③ **不要拿第二台设备当目标机**（此前出现过 `PA921BMGL3190210G`）；
> ④ 每项做完**当场勾**，别攒着；⑤ 未做的项**不许**在任何文档里写成「已验收」。

### B0 · 开工前置（每次都要）

- [ ] `adb devices` 里出现 `df97f3c4`，且 `getprop ro.product.model` = `25098PN5AC`
- [ ] 装包：`./gradlew :app:assembleRelease` → `adb -s df97f3c4 install -r app/build/outputs/apk/release/app-release.apk`
- [ ] 发布前核对签名：`apksigner verify --print-certs`（指纹应为 `ed78235d2945d2c075b5b1ce92c3eb8ab23222382007fb87dd7cc3f32a9dffb1`）
- [ ] 切到 Compose UI：`adb -s df97f3c4 shell am start -n com.luzzymeow.luzzyrp/.ui.ComposeActivity --activity-clear-top`，
      然后**核对前台**：`dumpsys activity activities | grep topResumedActivity`（曾出现请求被投给 WebView 的 `MainActivity`）

### B1 · 行内高亮目视证据（上一轮唯一缺的东西）

| 步骤 | 预期 | 判据 |
|---|---|---|
| 1. 先问用户能否往会话里发一条测试消息 | 用户同意 | **未同意就不做**（真机数据是用户的真实数据） |
| 2. 发一条含行内标签的消息（如 `<span style="color:#c9a227">「今晚有雨」</span>`，或让用户自己发带引号的消息并确认其正则集含高亮脚本） | 引号内文字**变色**、正文不断行、不吞字 | 截图/录屏留存；与 WebView 版同一条消息对照 |
| 3. 亮/暗两套主题各看一次 | 高亮色在两套下都可读 | 截图各一张 |
| 4. 发一条含 `<ui_template_updates>{…}</ui_template_updates>` 的消息 | **块不上屏**（正文干净），不报错 | 截图；对照 A2 的单测结论 |
| 5. 发一条含 `嘴角勾起一抹弧度`/`不容置疑` 的消息 | 短语**被删掉**且正文通顺（无孤立标点） | 截图；这是 A4 的目视判据 |
| 6. 发一条含 `<thinking>…</thinking>` 的消息（若模型会输出） | 思维链**不混进正文** | 截图；半条缺陷 #4 的目视一半 |

### B2 · 批 C 页面可见性验收（**不能只看数据对不对**）

| 步骤 | 预期 | 判据 |
|---|---|---|
| 1. 角色卡页 | 真角色名 + 真头像（无头像时首字 monogram） | **`getBoundingClientRect().width > 0` 语义等价**：Compose 用 `assertIsDisplayed()` 已覆盖，但**真机上还要看**头像是否被裁切/拉伸 |
| 2. 记忆页 | 分片数/覆盖轮数/已嵌入来自真库；空库时说「还没有记忆」 | 数字与 `LuzzyRegex`/日志口径一致；**看不见 = 红**（对照会话 59 的教训） |
| 3. 用量页 | 总用量/输入输出/按天折线/供应商行 | 与真实发过请求后的库数据一致；折线在**单点数据**时不画成一条退化线 |
| 4. 亮/暗两套主题各截一次图 | 无白块、无对比度过低 | 截图留存 |
| 5. 设置页（若本轮改了） | 文案与真实状态一致 | 截图 |

### B3 · A1 之后的缓存命中率复核（**本条是 A1 的验收尾巴**）

| 步骤 | 预期 | 判据 |
|---|---|---|
| 1. 连续发 5 轮以上真实对话 | 每轮日志出现「本轮 轮 N · 公共前缀 … · 命中 …」 | 口径同批 A（`docs/PLAN-v3.0-p5-agent-loop.md` §11.2） |
| 2. 读公共前缀占比 | **≥ 0.95**（目标 1.0000） | 若低于 0.95：先确认**用户是否配了「仅提示词」+ 深度区间**的脚本——那会让前缀**本就**漂移（上游同语义，见 `RegexScripts.applyToPromptMessages` 的说明与 `RequestBuilderTest` 的负面用例）；**那不是实现缺陷** |
| 3. 读 `cached_tokens / prompt_tokens` | 与批 A 同量级（批 A 实测 89%） | 明显下降 → 记下来并定位是哪条脚本 |
| 4. 负控：故意改一次设置 | 当轮占比掉到 0.5 以下 + 记一次 `headerChanged` | 与 `BatchACacheAcceptanceTest` 同口径 |

### B4 · 仪器化与长跑

| 步骤 | 预期 | 判据 |
|---|---|---|
| 1. `./gradlew checkChat`（需设备；**不要用模拟器**——用户纪律） | 全绿 | 含新增的 `PageDataUiTest` / `MessageBodyUiTest` |
| 2. B6 重试真机触发 | 溢出→先压缩再重发 / 网络→原样重发，各一次 | 目前只能靠 JVM 12 例，真机仍无法按需触发 |
| 3. 长回复跟随 | 流式期间自动贴底、划到底吸附 | 对照会话 74 的跟随链修复 |
| 4. 「同会话 10 张卡」滚动帧率 A/B | 卡片密集时不掉帧 | **成对交替测量**（A/B/A/B），小样本不可信（§7 坑表） |
| 5. 息屏唤醒 / 后台恢复 | 卡片与正文仍在 | 截图 |

### B5 · P6 切换与发版（**发版动作，用户必须在场**）

- [ ] 切 launcher（`AndroidManifest` 的 MAIN/LAUNCHER 指向 `ui.ComposeActivity`）
- [ ] 移除/保留 WebView 路径的决策（按 P6 计划）
- [ ] 单 APK + 签名一致（AGENTS §3.4 全流程）
- [ ] 真机回归（B1+B2 全过）
- [ ] **不要在无真机时做这一步**

### B6 · 用户拍板项（已决，执行时照办）

- [x] **C2 文风过滤**：用户 2026-09-14 拍板「做，默认开，照上游」→ 已实现（A4）
- [x] **C3 NAI 生图正则**：用户拍板「显式跳过」→ 已实现（`RegexScripts.IMAGE_GEN_UNSUPPORTED`）
- [ ] **C1 主分支里那条压缩水位线是否清掉**：用户拍板「**清掉**」——
      但**这是写真机数据的操作**，必须等设备回来、且执行前**先复述将执行的命令**再动手。
      届时确认：清掉水位线 = 恢复被取代的那段历史（**它之后的消息会随之处理，有丢消息风险**）
      → 执行前先**导出一份会话备份**（或让用户在应用内确认该会话可以重置）。

> ⚠️ **2026-09-14 会话 77 追记（重要，影响本节多数条目的口径）**：
> 设备已交还，但本轮**时间全部花在「让仪器化测试真正跑起来」**上，**B1/B2/B3 一条都没做**。
> 同时把 **B4 的仪器化部分做掉了**（改用模拟器，80 条真正跑通）——
> 也就是说 **B4 不再是全未做**，只有「重试触发 / 长回复跟随 / 10 张卡帧率」还欠着。
> **请以 `docs/HANDOFF-p5-instrumented.md` 为准**（它更新、更具体），本节只作清单底稿。

### B4 修正（会话 77 实测）

- [x] **`checkChat` 在模拟器上跑通**：`ANDROID_SERIAL=emulator-5554 ./gradlew checkChat`，
      **80 条 / 3 分 22 秒**（对比真机：几十分钟 0 条）。
      最后一次完整跑 78 过 / 2 失败 → 那 2 条已修（待补终值，见 `HANDOFF-p5-instrumented.md`）。
- [ ] B6 重试真机触发、长回复跟随、「同会话 10 张卡」帧率 A/B —— 仍未做。


## 交接清单（会话 76 收尾核对）

| 项 | 状态 |
|---|---|
| JVM 门禁 | ✅ **751 条 / 0 失败 ×3**（会话 76 起点 647，本轮 +104） |
| 批 A（请求组装/前缀缓存/观测） | ✅ 真机验收达标（公共前缀 1.0000、命中 89%） |
| 批 B B1-B7 | ✅ 代码完成；B5 真机跑通；B6 真机无法按需触发 |
| §26 块级 HTML 直通（卡片） | ✅ 真机目测通过（亮/暗双主题、滚动往返、唤醒） |
| §27 显示期正则 + 行内 HTML | ⚠️ 代码 + 单测完成并接线，**目视未做**（B1） |
| A1-A4（提示词正则 / 变量块 / CoT / 文风过滤） | ✅ 本轮完成（代码 + 单测 + 接线）；真机判据见 B1/B3 |
| A5 批 C 取数层与纯逻辑 | ✅ 聚合 + 取数 + 三页面接真库；**页面视觉未验收**（B2） |
| A6 仪器化用例 | ✅ 新增 16 例；**会话 77 已在模拟器上真正跑通**（80 条 / 3 分 22 秒） |
| A7 真机回归清单 | ✅ 已扩成可勾选清单（见上） |
| 批 C 余项（C3 多候选落库 / C4 附件 / C5 表格列宽 / C6 撤工作区 / C7 reduced-motion） | ⏳ **会话 78 开工**（逐项落点与状态见 WORKLOG 会话 78 节点） |
| 批 D（字号/导入导出/迁移报告页） | ⏳ **会话 78 开工**（同上） |
| 真机 | ⚠️ **已交还**（会话 77 起未再连接）；机上 = release（数据已清空）+ debug + 测试件（**后两者待用户决定去留**） |
| launcher / 发版（P6） | ❌ 仍指向 WebView `MainActivity` —— **这就是用户「点图标进 WebView 界面」的根因** |

> **会话 77 补充**：仪器化 80 条已真正跑通（模拟器），并借此修掉 3 处缺陷
> （`PageDataUiTest` 包名错、`SettingsStoreTest` 过期断言、`ChatPage.pinToBottom()` 测量帧重入）。
> **这 3 个文件已于会话 78 补验并提交**（`8b474e67`）。
>
> **会话 78 补充**：`checkChat` 终值 **80 条 / 0 失败**（冷启动模拟器后 2 分 5 秒）+ JVM 751/0；
> 门禁又抓出**第 4 处缺陷**（面板用例等待方式饿死帧 —— 测试写法，非产品缺陷）并已修；
> 真机设置还原**仍待做**（设备未连接）。详见 `docs/HANDOFF-p5-instrumented.md` 与 WORKLOG 会话 78 节点。


## 本轮**没有**做的事（如实登记，别当成已完成）

1. **任何视觉验收**：无真机 → 本轮所有「好看 / 看得见 / 颜色对」都是未验收状态；
2. **`renderUiTemplateHtml` 面板**（按指示未做——它要真机）；
3. **变量块的应用**：本轮只**剥除 + 解析**，解析结果**没有消费方**（模板变量面板未做）。
   所以 `stripAndParse` 的 `Parsed` 目前只有测试在用——这是有意的半步，
   写在这里是免得下一轮把它当成「已经能用了」；
4. **文风过滤的命中高亮**：只删、不高亮（高亮是视觉产出，要过设计门 + 真机）；
5. **批 C 的清空记忆按钮**：页面仍只有按钮与确认文案，**真执行未接**（写操作要真机验证）；
6. **NAI 生图管线**：显式跳过，不实现（`IMAGE_GEN_UNSUPPORTED` 是那个开关）。

