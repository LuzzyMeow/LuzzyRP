## LuzzyRP v1.4.0 — 同步上游 1.9.2 · 剧情面板 · 沉浸模式 · 抗截断协议融合

> 上游基线 RP-Hub **1.9.2**（`d2f2625`）· versionCode 12 · 稳定版（附 APK 三件套）

### 同步上游 1.9.2

- **UI 实时生成（剧情面板）**：新增 `story_panels` 预设——随剧情主动插入手机界面 / 便签 /
  信件等 HTML 面板，渲染链完整保留（预设默认关闭，需在预设中启用）
- **沉浸模式**：`settings.immersiveMode`（消息居中无头像布局），设置页可切换
- **角色卡牌组**（CharacterDeck 组件）· **快捷面板**样式与密度重构
- **主动工具调用改原生 toolCalls 协议**（原文本标签解析退役）
- **抗截断改 `output_reply` 工具协议**：采纳上游 `replyInTool` 原生流式解析（仅 Gemini
  模型可见，与上游实现一致）；我方三协议适配器只做协议分派 + `max_tokens` / `extraBody` 注入
- 上游修复采纳：正则渲染嵌套重复渲染 / UI 生成状态下正文异常阻断 / 新手引导高度自适应
- 安全面全过：`nsfw` 对象与上游逐字节一致、vendor/ 零变化、novel 与 runtime-services 零变化

### 新增

**关于页更新日志关键词高亮（patch 039）**
- 检索命中的内容里关键词以 `<mark>` 高亮（文本节点级遍历，不破坏结构；大小写不敏感）
- 底色取设计真源 highlight token `#F5D9A8`（与开屏荧光笔记号同源），暗色下自动配反转主文字

### 优化

**用量统计页时间筛选去冲突（patch 037）**
- 删除右上角「更多」下拉（全部 / 24小时 / 7天 / 30天）——与折线图「日 / 周 / 月」粒度
  语义冲突；保留折线图粒度与「全部 / 主对话 / 记忆系统 / 变量分析」类型筛选

**版本更新公告品牌化（patch 038）**
- 公告弹窗标题由上游「网站公告」改为品牌名 **LuzzyRP**
- 内容区底部新增同步来源注释：**同步更新上游节点**：本公告内容随上游 RP-Hub 版本同步，
  由 LuzzyRP 呈现。

**开屏「开卷」恢复（会话 25）**
- 修复同步过程中开屏区被上游书本动画与我方残骸混合覆盖的问题——七个结构节点与标记
  全部恢复，`entry-transition` 彻底退役

### 修复

- **实体重放通道加固（工具层）**：实体段先于字符串块执行、前像判定改用实体头 `index pre`
  的 blob id、`git apply` 告警不再中断脚本、应用后强制校验标记落盘——仓库外逆向 9/9、
  纯净基线端到端重放 9/9 全过
- 补回 index.html 的 001 / 004 / 006 标记注释并删除残留的 Google Fonts preconnect
  （字体本地化合规）

### 注意事项

- **新功能默认值**：沉浸模式默认关闭、剧情面板预设默认关闭、角色卡牌组默认关闭（原样交付，
  体验后再议）
- **抗截断**：仅 Gemini 模型可见（上游实现即如此）
- **开屏**：仍为 LuzzyRP 自创「开卷」，不采纳上游书本动画
- **全屏**：聊天页全屏功能继续下线（沿用 v1.2.3 用户决策）
- 剧情面板 / 沉浸模式 / 角色卡牌组暂以 classic 样式交付，主题化定制待体验后按设计流程推进
- 门禁：verify-markers **82 PASS / 0 FAIL**；实体 9 枚以 1.9.2 基线再生成（逆向 9/9 PASS）；
  全 JS `node --check` 13/13；`nsfw_rules` 与上游逐字节一致
